info:
  name: POST /api/v1/actors
  type: http
  seq: 3

http:
  method: POST
  url: "{{host}}/api/v1/actors"
  body:
    type: json
    data: "{\r

      \    \"name\": \"Jos Bosmans\"\r

      }"
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: bru.setCollectionVar("actor_id", res.getBody().id);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
