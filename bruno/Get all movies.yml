info:
  name: Get all movies
  type: http
  seq: 6

http:
  method: GET
  url: "{{host}}/api/v1/movies"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
